info:
  name: login traveler
  type: http
  seq: 3

http:
  method: POST
  url: http://0.0.0.0:8000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "cocox@yopmail.com",
        "password": "chepper"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
