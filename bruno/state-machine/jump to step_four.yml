info:
  name: jump to step 4
  type: http
  seq: 6

http:
  method: POST
  url: http://0.0.0.0:8000/tasks/1/jump
  body:
    type: json
    data: |-
      {
        "target_step": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
