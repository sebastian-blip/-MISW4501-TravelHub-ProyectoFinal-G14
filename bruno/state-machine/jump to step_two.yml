info:
  name: jump to step 2
  type: http
  seq: 4

http:
  method: POST
  url: http://0.0.0.0:8000/tasks/4/jump
  body:
    type: json
    data: |-
      {
        "target_step": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
