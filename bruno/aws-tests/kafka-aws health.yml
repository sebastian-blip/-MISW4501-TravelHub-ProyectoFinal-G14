info:
  name: kafka-aws health
  type: http
  seq: 1

http:
  method: GET
  url: http://0.0.0.0:8000/test/kafka-aws/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
