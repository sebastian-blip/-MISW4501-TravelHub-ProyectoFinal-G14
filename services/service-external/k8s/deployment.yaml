apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-external
  labels:
    app.kubernetes.io/name: service-external
    app.kubernetes.io/part-of: travelhub
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: service-external
  template:
    metadata:
      labels:
        app.kubernetes.io/name: service-external
    spec:
      containers:
        - name: app
          image: service-external:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - secretRef:
                name: service-external-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
