info:
  name: transition to cancelation
  type: http
  seq: 6

http:
  method: POST
  url: http://0.0.0.0:8000/tasks/1/transition
  body:
    type: json
    data: |-
      {
        "target_state": "cancelation"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
