info:
  name: test
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8001/validate/db-check
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
