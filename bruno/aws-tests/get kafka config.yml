info:
  name: get kafka config
  type: http
  seq: 6

http:
  method: GET
  url: "{{local_test}}/config/kafka"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
