info:
  name: list cities
  type: http
  seq: 4

http:
  method: GET
  url: http://0.0.0.0:8000/accommodations/cities
  auth: bearer
  bearer:
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
