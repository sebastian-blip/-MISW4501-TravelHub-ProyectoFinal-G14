info:
  name: kafka-aws health
  type: http
  seq: 1

http:
  method: GET
  url: "{{core}}/test/kafka-aws/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
