info:
  name: go back
  type: http
  seq: 8

http:
  method: POST
  url: http://0.0.0.0:8000/tasks/1/go-back
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
