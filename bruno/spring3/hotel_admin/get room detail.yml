info:
  name: get room detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_core}}/service-core/hotel-admin/rooms/{{room_type_id}}"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
