info:
  name: get user profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_core}}/users/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
