info:
  name: list all reservations
  type: http
  seq: 2

http:
  method: GET
  url: http://0.0.0.0:8000/reservations?limit=10&offset=0
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
