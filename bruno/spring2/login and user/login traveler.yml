info:
  name: login traveler
  type: http
  seq: 3

http:
  method: POST
  url: "{{local_core}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "juanpd.26@gmail.com",
        "password": "Juanjose.pd20@"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
