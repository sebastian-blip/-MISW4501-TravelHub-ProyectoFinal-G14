info:
  name: execute function
  type: http
  seq: 7

http:
  method: POST
  url: http://0.0.0.0:8000/tasks/1/execute
  body:
    type: json
    data: |-
      {
        "function_name": "check_user_exists"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
