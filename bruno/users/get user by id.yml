info:
  name: get user by id
  type: http
  seq: 3

http:
  method: GET
  url: http://0.0.0.0:8000/users/{{user_id}}
  auth: bearer
  bearer:
    token: "{{token}}"

runtime:
  variables:
    - name: user_id
      value: 50ddff9a-b40d-4ce3-aaff-5c8fd736de1e

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
