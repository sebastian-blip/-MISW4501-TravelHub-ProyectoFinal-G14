apiVersion: v1
kind: Service
metadata:
  name: pms
  labels:
    app.kubernetes.io/name: pms
spec:
  selector:
    app.kubernetes.io/name: pms
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
